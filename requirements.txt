tree-sitter>=0.21.0
tree-sitter-java>=0.21.0
pandas>=2.0
openai>=1.0
tqdm>=4.60
pyahocorasick>=2.0
# Filter 3 model-mode (Qwen2.5-Coder-0.5B fine-tuning) is an optional
# install. To enable it run:
#   pip install -e ".[coverage]"
# which adds: transformers>=4.40, torch>=2.1, datasets, scipy, scikit-learn.
